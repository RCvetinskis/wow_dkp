import GuildList from "@/components/lists/guild-list";

type Props = {};

const GuildsPage = (props: Props) => {
  return (
    <div>
      <GuildList />
    </div>
  );
};

export default GuildsPage;
